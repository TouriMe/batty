.section.comments#comment-section
  h4
    i.fa.fa-list-ul[aria-hidden="true" ]
    = pluralize(comments.count, 'comment')
  - comments.each do |c|
    .tag.comment
      .small-6.columns
        = c.message
      .small-6.columns
        .section.rating
          - c.rating.times do
            i.fa.fa-star
          - (5 - c.rating).times do
            i.fa.fa-star-o
      p
        | From:&nbsp;
        = c.tourist.name

  - if tourist_signed_in?
    = form_for [driver, comments.new], url: {action: 'add_comment'} do |f|
      = f.text_area :message
      .rating
        label(for='driver_comment[rating]') Rating
        = f.select :rating, (1..5).map{|i| [i,i]}.reverse
      = f.submit 'Add Comment'

